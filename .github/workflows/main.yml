name: Build TWRP KE7 V472
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: |
          sudo apt install -y unzip
          mkdir -p ~/.bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/.bin/repo
          chmod a+x ~/.bin/repo
          echo "$HOME/.bin" >> $GITHUB_PATH

          mkdir ~/twrp && cd ~/twrp
          repo init -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp.git -b twrp-12.1 --depth=1
          repo sync -j2 --force-sync

          echo "=== فك ملفك KE7-tree.zip ==="
          ls -lh $GITHUB_WORKSPACE/KE7-tree.zip
          rm -rf /tmp/ke7 && mkdir /tmp/ke7
          unzip -o $GITHUB_WORKSPACE/KE7-tree.zip -d /tmp/ke7
          find /tmp/ke7 -type f | head -20

          DEV_DIR=$(find /tmp/ke7 -name "BoardConfig.mk" | head -1 | xargs dirname)
          echo "DEV_DIR=$DEV_DIR"
          
          rm -rf device/infinix/KE7
          mkdir -p device/infinix/KE7
          cp -r $DEV_DIR/* device/infinix/KE7/
          
          # اصلاح الـ theme اللي كان موقفك عند 99%
          if ! grep -q "TW_THEME" device/infinix/KE7/BoardConfig.mk; then
            echo "TW_THEME := portrait_hdpi" >> device/infinix/KE7/BoardConfig.mk
          fi
          
          ls device/infinix/KE7/

          . build/envsetup.sh
          lunch twrp_KE7-eng
          mka recoveryimage -j4

      - uses: actions/upload-artifact@v4
        with:
          name: recovery-KE7-V472-FIXED
          path: ~/twrp/out/target/product/KE7/recovery.img
